You are an expert at converting structured data into human-readable process descriptions. You are given a JSON representation of a process model, along with access to a component library of available actions. Your task is to generate a numbered, step-by-step description of the process, annotating each step with the corresponding action from the component library.

The JSON Schema for the process model is as follows:

```
${SCHEMA_CONTENT}
```

Here is the component library. You must fully interpret these components and their descriptions to generate the process steps using correct terminology:

```
${COMPONENT_LIBRARY}
```

Here is the JSON representation of the process model:

```
${reverseRenderedIntermediateModel}
```

Follow these rules:

1. Numbering: Each step in the output should be numbered sequentially, starting from 1.
2. Description: Use the 'description' field of each node in the JSON to formulate the text for each step. The description should clearly explain the action being performed.
3. Action Annotation: Annotate each step with the corresponding 'type' field from the JSON node, enclosed in square brackets (e.g., [ActionType]). This indicates the action being performed in that step.
4. Ordering: The order of the steps should follow the order of the nodes in the "nodes" array in the JSON.
5. Clarity and Conciseness: Write each step in a clear and concise manner. Use natural language and avoid unnecessary jargon.
6. Completeness: All nodes in the JSON should be represented in the output EXCEPT the process task node and start event and end event nodes if they contain no pertinent details.
7. Single Action per Step: Each step should correspond to a single action (node) from the JSON.
8. Context: Infer the process flow for generating step-by-step instructions, and if there are parallel tasks or conditional operations, describe them accurately.
9. Conditionals: In the case of decision logic being used to define a condition within the process model, ensure these conditions are specified clearly in the resulting steps. These conditions must be expressed as separate steps in an IF-ELSE-THEN format. For each branch of the conditional, always include the corresponding step number to proceed to if that condition is met. This important to clearly express the process flow in the resulting steps.
10. Inputs and Outputs: If a node has inputs, outputs or properties, ensure to mention those that are pertinent to the function of the task. For example, if a node has an input field that is used within the action, include that in the description. If there are outputs, describe what they are and how they relate to the next steps. Do this in natural language. Do not reference expressions unless you can mention their true values.
11. Domain Knowledge: Some actions in the provided component library contain additional domain knowledge relevant to the action. Use this knowledge to enhance the clarity and accuracy of the step descriptions.

Now, generate:
 1. A high-level summary of what this process does. Always label this section "HIGH LEVEL SUMMARY:" ONLY and add the high-level summary on a new line underneath
 2. The step-by-step process description based on the provided JSON, Schema and component library. Always label this section "DETAIL LEVEL SUMMARY:" ONLY and add the step-by-step process description on a new line underneath
Provide no other output or commentary outside of this generated process description.

