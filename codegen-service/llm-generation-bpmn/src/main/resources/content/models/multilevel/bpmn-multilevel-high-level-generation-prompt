You are designing a business process model following the BPMN 2.0 standard.  We will generate the model in two stages
1. Build a HIGH-LEVEL model by connecting together appropriate BPMN 2.0 elements
2. Extend this into a DETAILED model by adding configuration and linking node inputs & outputs

Let us start with the HIGH-LEVEL model.  You can use any of the BPMN 2.0 elements in this component library:
```
${COMPONENT_LIBRARY}
```

You should generate a simplified graph model by returning JSON that complies with the following schema.  You should NOT return a response in JSON Schema format.  Rather you should return raw JSON, which COMPLIES WITH this schema:
```
${SCHEMA_CONTENT}
```

Other requirements:
* You can only use elements specified in the component library above
* There must be EITHER one processConfig node or one subprocess node. It should have no inbound or outbound connections. ProcessConfig / subprocess nodes are the only nodes allowed to be an orphan. If the schema above is a subprocess (SP) (e.g. contains the steps 'SP1.1' for example) then DO NOT include a processConfig node, and instead use the element node type 'subProcess'. Otherwise, use a processConfig node.
* Node IDs and names must be descriptive of what the node does. Do NOT use generic index-based names like `sp1_task1` or `SP2_step3`.
* A node can only have ONE inbound and ONE outbound connection, UNLESS it is an 'exclusiveGateway' node.  As a result, you should use exclusiveGateway nodes to handling branching. They should be placed after a result is calculated which you want to branch on. If you need to merge branches, use another 'exclusiveGateway' node with no conditions.
* Your process should begin with a Start Event and end with one or more End Events
Each non-endEvent can make outbound connections to other nodes. These connections should reference the 'name' of the node they are connecting to.
  ** The component library specifies which connections are required for each node type. endEvent nodes should have no outbound connections.
  ** If a non-endEvent node has an outbound connection to an endEvent node, ensure that the endEvent node has a unique name and id.
* All steps are part of the SAME process.  You should make sure that steps in your model are correctly linked together to form a single process.  There should be no orphaned steps or processes - every step must be linked.
* Nodes should indicate how each of its inputs will be satisfied. Component library defines allowed values for each input which can be one of the following:
  1. A constant value.  `source` should be the value, `sourceType` should be "CONSTANT"
  2. A Groovy expression.  `source` should be the expression, `sourceType` should be "EXPRESSION"
  3. A Groovy script.  `source` should be the script, `sourceType` should be "SCRIPT"
* The input values MUST be found in the 'requiredInputs' array for the node - do not include any inputs that are not part of this requiredInputs array for the node in question.
* Node inputs follow two valid shapes:
  1) **Leaf input** (direct assignment): Has `name`, `source`, and `sourceType`. No `properties`.
  2) **Object input** (structured assignment): Has `name` and `properties` (array of sub-inputs). No `source` or `sourceType`.
     - Use object inputs when the component library defines an input with nested properties.
     - Each property within `properties` is itself an input that follows these same two shapes recursively.
     - Object inputs MUST contain at least one leaf input somewhere in their `properties` subtree.
  3) **Array input** (type = array): You MAY include MULTIPLE input entries with the SAME `name`; each entry represents ONE array element.
     - For arrays of objects: each array element MUST be an object input (has `properties`).
     - For arrays of primitives: each array element MUST be a leaf input (has `source` and `sourceType`).
* Do not use `source`/`sourceType` and `properties` on the same input — they are mutually exclusive

Start and End Event RULES:
* If event start events (messageStartEvent, timerStartEvent, errorStartEvent, conditionalStartEvent) are used inside subprocesses they must set triggeredByEvent as true. They can also be used in the main process, but only if the process itself is triggeredByEvent=true.
  - They replace the normal startEvent inside an event-driven subprocess or main process.
* terminateEndEvent terminates the entire process instance. Use instead of endEvent when all active branches must stop.
* errorEndEvent throws an error that can be caught by an errorStartEvent in an event subprocess. Use inside subprocesses to propagate errors.
* Intermediate events (messageIntermediateCatchEvent, messageIntermediateThrowEvent, timerIntermediateCatchEvent) sit in the main process flow like regular nodes with inbound and outbound connections.
* Do NOT include boundary events or iteration config in this high-level model. Do NOT add an "events" array or "iterationConfig" property to any node. Boundary events and iteration configs will be added during the detail-level generation stage.

<#if GLOBAL_VARIABLE_LIBRARY?has_content>
These are the global variables which can be used within EXPRESSION or SCRIPT inputs: `getGlobalVariable('variableName', [arg1, arg2])`
```
${GLOBAL_VARIABLE_LIBRARY}
```

<#else>
No global variables are available at the moment. Inputs should be satisfied using constant values, expressions, or scripts.
</#if>

<#if STARTING_PAYLOAD_VARIABLES?? && STARTING_PAYLOAD_VARIABLES?has_content>
These are the initial payload variables available at the start of the process that can be used to satisfy inputs:
```
${STARTING_PAYLOAD_VARIABLES}
```
</#if>

<#if highLevelModel??>
You have previously generated the following model, which follows the schema above:
```
${highLevelModel}
```
    <#if llmDirectedRetryReason??>
    However the agent responsible for building the detail-level model based on your model reported that it is unable to do so, for the following reason:
    ```
    ${llmDirectedRetryReason}
    ```
    Please make corrections/updates to your model to address these reported problems.
    <#else>
    You should make the following updates to this model, and return the new full model as JSON (complying to the schema above):
    ```
    ${request}
    ```
    </#if>
<#else>
The model which you should generate in JSON form (complying to the given schema) is as follows:
```
${request}
```
</#if>
Return ONLY the JSON process data, with no other explanation or commentary.